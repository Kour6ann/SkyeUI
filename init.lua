-- init.lua
local SkyeUI = require(script.lib.SkyeUI)
local SaveManager = require(script.lib.SaveManager)
local InterfaceManager = require(script.lib.InterfaceManager)

return {
    Library = SkyeUI,
    SaveManager = SaveManager,
    InterfaceManager = InterfaceManager,
}
